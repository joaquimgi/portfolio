import React from "react";

const navHelper = ({ background }) => {
  return (
    <div
      style={{
        backgroundColor: background,
        width: "100%",
        height: "100px",
      }}
    />
  );
};

export default navHelper;
