import React from "react";

const SectionContent = ({ title, description }) => {
  return (
    <div style={{}}>
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
};

export default SectionContent;
